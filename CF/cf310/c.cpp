#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define vi vector<int>
#define vll vector<ll>


int main()
{
	ios_base::sync_with_stdio(0);
	string s;
	cin >> s;
	int l = 0 ;
	for(int i =0 ; i < s.size() ; i++){
		if(s[i] != s[i+1])
	}
	
	return 0;
}
