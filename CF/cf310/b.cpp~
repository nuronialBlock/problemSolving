#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define vi vector<int>
#define vll vector<ll>


int main()
{
	ios_base::sync_with_stdio(0);
	string s ;
	cin >> s;
	
	int siz = s.size();
	cout << (26 * (siz+1)) - siz << endl;
	return 0;
}
