#include <bits/stdc++.h>

using namespace std;

#define pii pair<int , int >
#define ff first
#define ss second

#define LL long long 
#define uLL unsigned long long

#define vi vector<int > 

int main()
{
	ios_base::sync_with_stdio(0);
	int n;
	int ans = 1;
	cin >> n;
	for(int i = 0 ; i < n ; i++){
		int  k;
		cin >> k;
		if(k == 1) ans = -1;
	}
	
	cout << ans << endl;
	
	return 0;
}
